{
  "name": "react",
  "version": "0.0.0",
  "description": "Module Federation Vite + React is now possible",
  "scripts": {
    "install:deps": "pnpm --filter react-* install",
    "postinstall:deps": "pnpm --filter react-* run build",
    "dev": "pnpm --filter react-* dev",
    "build": "pnpm --filter react-* build",
    "preview": "pnpm --filter react-* preview"
  }
}
