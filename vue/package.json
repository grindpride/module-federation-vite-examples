{
  "name": "vue",
  "description": "Production-oriented Vue 3 host with nested Module Federation remotes",
  "scripts": {
    "dev": "pnpm --filter vue-* dev",
    "build": "pnpm --filter vue-* build",
    "preview": "pnpm --filter vue-* preview"
  }
}
