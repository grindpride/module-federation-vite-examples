{
  "name": "vue",
  "version": "1.0.0",
  "description": "Module Federation Vite + Vue3 is now possible",
  "scripts": {
    "install:deps": "pnpm --filter vue-* install",
    "postinstall:deps": "pnpm --filter vue-* run build",
    "dev": "pnpm --filter vue-* dev",
    "build": "pnpm --filter vue-* build",
    "preview": "pnpm --filter vue-* preview"
  }
}
